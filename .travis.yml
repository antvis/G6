language: node_js

node_js:
  - '12'

env:
  - NODE_ENV=test

install:
  - yarn

script:
  - |
    if [ "$TEST_TYPE" = test ]; then
      yarn ci && cat ./coverage/lcov.info | rm -rf ./coverage
    else
      yarn $TEST_TYPE
    fi
env:
  matrix:
    - TEST_TYPE=lint
    - TEST_TYPE=test
