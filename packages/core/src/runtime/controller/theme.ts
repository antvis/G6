import { BehaviorRegistry } from "../../types/behavior";

/**
 * Manages theme extensions for graph.
 * Themes are the mapper from item's inner model to display model.
 */
export class ThemeController<B extends BehaviorRegistry> { }