import { ElementController } from '../../../../src/runtime/controller/element';

describe('ElementController', () => {
  it('init', () => {
    const graph: any = {};

    const controller = new ElementController(graph);
  });
});
