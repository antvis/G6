# To see what each property does see here: https://yarnpkg.com/configuration/yarnrc

# Only allow git: dependencies from Siren repositories.
# Yarn 4.x requires explicit approval for git-hosted dependencies (enableStrictSettings default).
approvedGitRepositories:
  - "https://github.com/sirensolutions/*"

# Yarn 4.17+ defaults enableScripts to false. We need lifecycle scripts (postinstall, etc.)
# for native addons and build steps, so this must be explicitly enabled.
enableScripts: true

enableTelemetry: false

nodeLinker: node-modules

# Age gate can be bypassed by setting YARN_NPM_MINIMAL_AGE_GATE=0 env variable.
npmMinimalAgeGate: 10080 # 7 days

npmPublishRegistry: "https://europe-west1-npm.pkg.dev/siren-cicd/npm-standard/"

npmScopes:
  antv:
    npmRegistryServer: "https://europe-west1-npm.pkg.dev/siren-cicd/npm-virtual/"

plugins:
  - checksum: 7a993ef2944928530db74ca2f844dc21324bdc768bb0744b772277b114f28da4625903f4109fc90f3de6621e97c646f8969167df3435ee7e443d79b8a4806a62
    path: .yarn/plugins/@sirensolutions/yarn-plugin-gar-auth.cjs
    spec: "@sirensolutions/yarn-plugin-gar-auth.cjs"

yarnPath: .yarn/releases/yarn-4.17.1.cjs
